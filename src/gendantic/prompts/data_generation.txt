Generate {count} realistic synthetic records for the {model_class} model.

**CRITICAL**: This model has field validators that ABSOLUTELY MUST be respected. 
ANY data that fails validation will be rejected. Validator requirements override context preferences.

**MANDATORY VALIDATION REQUIREMENTS** (MUST FOLLOW):
{critical_requirements}
{validator_hints}
{additional_requirements}

**MODEL CONTEXT**:
- **PURPOSE**: {purpose}
- **DOMAIN**: {domain} 
- **USE CASE**: {use_case}
- **CONTEXT**: {context}

**GENERATION STRATEGY**: {generation_strategy}

**FIELD RELATIONSHIPS**: {field_relationships}

**FIELD-SPECIFIC GUIDANCE**:
{field_guidance}

**GENERATION RULES (in order of priority)**:
1. **VALIDATOR COMPLIANCE**: ALL generated data MUST pass Pydantic field validators (highest priority)
2. **CONSTRAINT COMPLIANCE**: Follow all field types, min/max values, string patterns
3. **CONTEXT ADAPTATION**: Adapt to the given context while respecting validators
4. **INTERNAL CONSISTENCY**: Fields should logically relate to each other
5. **CULTURAL AUTHENTICITY**: Ensure appropriate diversity and cultural considerations

   WARNING: If there is ANY conflict between context and validators, ALWAYS choose validator compliance.

Generate diverse, realistic data that will 100% pass Pydantic validation.